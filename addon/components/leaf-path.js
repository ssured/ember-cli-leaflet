import LeafILayerComponent from '../components/leaf-ilayer';
import LeafStyleComponent from '../mixins/leaf-style';

export default LeafILayerComponent.extend(LeafStyleComponent, {
  // layerEvents: 'click dblclick mousedown mouseover mouseout contextmenu add remove popupopen popupclose'.w(),
});

